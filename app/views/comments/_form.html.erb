<%= form_with(model: @comment, url: project_disqux_comments_path(2,1), class: "new_comment",  local: true) do |form| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% @comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">  
    <%= form.hidden_field :user_id, value: @user %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, :class => 'text_area' %>
  </div>

  <div class="field">        
    <%= form.hidden_field :disqux_id, value: @disqux_id %>
  </div>
  

  <div class="actions"> 
  <%= form.submit 'Create comment', :class => 'btn btn--primary flush--bottom' %>
  
  </div>
<% end %>


